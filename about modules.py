"""
Modules are predefined line of codes that when imported, makes the program easier and shorter.
"""
import random   # This will print the random number. To checkout its code, hold ctrl and do a mouse click.
r = random.randint(0,7)
print(r)